<%= form_with(model: txn, local: true) do |form| %>
  <% if txn.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(txn.errors.count, "error") %> prohibited this txn from being saved:</h2>

      <ul>
      <% txn.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :txnid %>
    <%= form.text_field :txnid, id: :txn_txnid %>
  </div>

  <div class="field">
    <%= form.label :from %>
    <%= form.text_field :from, id: :txn_from %>
  </div>

  <div class="field">
    <%= form.label :block_id %>
    <%= form.text_field :block_id, id: :txn_block_id %>
  </div>

  <div class="field">
    <%= form.label :label %>
    <%= form.text_field :label, id: :txn_label %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address, id: :txn_address %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
